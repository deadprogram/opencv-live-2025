main:
  source: "2"
  logging: "info"
  destination: ":8080"
  cuda-enable: true

processing:
  pipeline:
    - "ollama.wasm"
    - "style-transfer.wasm"
    - "captions.wasm"

configuration:
  ollama-model: "qnguyen3/nanollava"
  ollama-prompt: "Describe this scene in a complimentary way with a single short sentence."
  caption-line-height: "30"
  caption-size: "0.8"

models:
  download: true
